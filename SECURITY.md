# 安全政策

[English](SECURITY.en.md) · **繁體中文**

FamilyRecorder 處理家庭錄音、逐字稿與聲音特徵。安全問題在這裡的影響是具體的，我們認真看待。

---

## 支援版本

| 版本 | 狀態 |
|---|---|
| 0.14.x | ✅ 支援 |
| 0.14.0 以下 | ❌ 請升級 |

修正只會發布在最新版本上。

---

## 如何回報

> ⚠️ **請不要為安全問題開公開 issue。**

請使用 GitHub 的私密漏洞回報：

**[Security → Report a vulnerability](https://github.com/pcpcchen-coder/FamilyRecorder/security/advisories/new)**

這會建立一個只有維護者看得到的私密討論串。

### 請包含

- 問題的簡述與影響
- 重現步驟
- 受影響的版本與 macOS 版本
- 如果有的話：概念驗證

> 🔒 **回報時請先移除你自己的逐字稿內容、家庭成員姓名與檔案路徑中的使用者名稱。** 重現步驟通常不需要真實資料。

---

## 我們會怎麼處理

| 階段 | 目標 |
|---|---|
| 確認收到 | 7 天內 |
| 初步評估 | 14 天內 |
| 修正與發布 | 依嚴重程度而定 |

這是一個由個人維護的專案，沒有專職安全團隊。我們會盡力，但無法保證企業級的回應時間。

修正發布後，若你願意，我們會在 advisory 中致謝。

---

## 什麼算是安全問題

### ✅ 算

- **邊界被繞過**：任何讓音訊、聲音特徵或 SQLite 內容離開本機的路徑
- **認證外洩**：任何讓 ChatGPT／Codex token、Google 憑證進入 log、YAML、plist 或 process arguments 的路徑
- **提示注入生效**：逐字稿內容能讓摘要程式執行工具、讀取預期以外的檔案，或改變固定契約
- **權限提升**：讓 LaunchAgent 取得超過使用者層級的權限
- **解除安裝造成資料破壞**：移除到預期範圍以外的檔案
- **模型下載的完整性問題**：allowlist 或 GGML 驗證可被繞過
- **本機檔案權限**：`speaker-profiles/` 未以 `0600` 建立，或其他敏感檔案權限過寬

### ❌ 不算

| 情況 | 為什麼 |
|---|---|
| **能存取這台 Mac 的人可以讀到逐字稿** | 這是[已知且明說的限制](docs/privacy.md#這套設計不防護的)。請用 FileVault 與帳號密碼保護 |
| **備份包含資料目錄** | 同上。Time Machine／iCloud 的加密由你控制 |
| **同住者可以讀到彼此的逐字稿** | 這是共用電腦上的共用資料，屬於家庭內部信任問題 |
| **人別辨識可以被騙過** | 這**不是**身分驗證機制，[文件已明說](docs/speakers.md)。它不應被用於任何需要確認身分的用途 |
| **Whisper 辨識錯誤** | 品質問題，請開一般 issue |
| **DMG 未經 Apple 公證** | [已知且明說](docs/getting-started.md#關於-gatekeeper)。請核對 SHA-256 |

---

## 設計上的安全取捨

這些是**刻意的設計決定**，不是漏洞：

- 逐字稿以**明文**存在本機。加密會讓使用者無法用一般工具閱讀自己的記錄，且金鑰仍得存在同一台機器上。
- 聲音特徵**不加密**，但以 `0600` 保存且無法還原成聲音。
- 三個工作都是**使用者層級 LaunchAgent**，刻意不使用 root。
- 摘要路徑**沒有音訊上傳實作**，這是結構限制而非執行期檢查。

完整威脅模型見 [隱私與信任邊界](docs/privacy.md#威脅模型)。

---

## 你可以自己驗證的事

[隱私文件](docs/privacy.md#如何自己驗證)列出可以自己執行的檢查，包含用 `fs_usage` 觀察摘要執行時開啟了哪些檔案、確認逐字稿不在 process arguments，以及確認設定與 plist 中沒有金鑰。

發現與文件描述不符的行為，就是值得回報的問題。
